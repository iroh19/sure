fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
transformers>=4.44.0
torch>=2.3.0
accelerate>=0.33.0
peft>=0.12.0

# Apple Silicon: inference.py MPS görürse mlx backend'ine geçer ve mlx_lm
# import eder. Bu satır olmadan README'deki Mac quickstart'ı startup'ta patlar.
mlx-lm>=0.28.0 ; sys_platform == "darwin" and platform_machine == "arm64"

# 4-bit quantization yalnızca CUDA yolunda kullanılır; Apple Silicon'da
# kurulmaz (wheel yok) ve gerekmez — 1B model BF16'da ~2GB.
bitsandbytes>=0.43.0 ; sys_platform == "linux"
datasets>=2.21.0
trl>=0.10.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# ─── RAG ─────────────────────────────────────────────────────────────────────
# Bilgi tabanı retrieval'ı. rag/retriever.py bunların hiçbiri kurulu olmasa da
# çökmez — retrieve() boş liste döndürür ve sistem RAG'siz çalışır. Yine de
# üretimde kurulu olmaları beklenir.
sentence-transformers>=3.0.0
psycopg[binary]>=3.2.0
pgvector>=0.3.0
