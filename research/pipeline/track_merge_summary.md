# Track Merge Summary — S.U.R.E. Applied-Systems Paper (project_000)

**Role:** Track Merge Specialist. **Scope:** organize and faithfully consolidate what the
experiment track produced across all 11 goals (G1–G11); no re-evaluation of quality (that is
formalize_results/duality_check's job downstream). Every claim below traces to a specific source
file under `experiment_workspace/`.

## 0. Track Inventory

- **Theory track: intentionally not run.** `paper_workspace/track_decomposition.json`
  (generated by the research-goal-formalization-specialist agent) sets
  `"recommended_track": "empirical"` and `"theory_questions": []`, honoring
  `brainstorm.json`'s own `track_recommendation.needs_theory_track = false`. Rationale on record:
  the paper's only formalization needs — the `final_severity = max(severity_rule, severity_llm)`
  monotonicity property, the enumerable-vs-non-enumerable failure-mode distinction, the PSI-gating
  three-way rule, and the vision operating-point tradeoff framing — are expository restatements of
  already-implemented code, not new derivations, and were folded as short Methodology prose into
  G3, G7, and G8 rather than spun into a parallel proof track. Confirmed directly on disk:
  **no `math_workspace/` directory exists under `project_000/`.** This is a deliberate scope
  decision made at the brainstorm/formalize_goals phase, not a missing or broken artifact.
- **Experiment track: fully run.** `experiment_workspace/` contains `experiment_design.json`,
  `experiment_rationale.md`, three grouped execution logs (`execution_log_groupA/B/C.json`)
  consolidated into `execution_log.json`, `verification_report.md`, `verification_results.json`,
  `verification_handoff.md`, and 11 per-experiment run directories under `experiment_runs/`
  (EXP01–EXP11), each containing its own `results.md` plus raw output logs/JSON.

## 1. Headline Outcome

**8 pass (goal_satisfaction: strong) / 3 partial (goal_satisfaction: minimum_viable) / 0 fail**,
per `verification_results.json` and `verification_handoff.md`. No fabricated numbers, manufactured
significance, or hidden failures were found by verification — every metric independently
recomputed from raw output files on disk matched the corresponding `results.md` figure to within
rounding noise (largest observed discrepancy: 0.09% relative, EXP07's argmax-F1 table cell —
not conclusion-changing).

| Goal | Experiment | Verdict | Satisfaction |
|---|---|---|---|
| G1 | EXP01 | pass | strong |
| G2 | EXP02 | pass | strong |
| G3 | EXP03 | partial | minimum_viable |
| G4 | EXP04 | pass | strong |
| G5 | EXP05 | partial | minimum_viable |
| G6 | EXP06 | pass | strong |
| G7 | EXP07 | pass | strong |
| G8 | EXP08 | partial | minimum_viable |
| G9 | EXP09 | pass | strong |
| G10 | EXP10 | pass | strong |
| G11 | EXP11 | pass | strong |

## 2. Per-Goal Summary

### G1 — RAG Retrieval Calibration Reliability (EXP01, strong)
F1-argmax at threshold 0.84 (F1=0.951) vs. deployed 0.85 (F1=0.906), reproduced exactly from
`calibrate_output.log`. A new random/no-retrieval baseline (authored this run — `bench_readonly.py`
+ `random_baseline.py`, since `bench.py`'s own path writes to the live production DB and was
excluded by the read-only guardrail) shows e5-small/heading clears random by hit@1 +0.586
(0.793 vs 0.207) and MRR +0.438 (0.856 vs 0.418). Source: `experiment_runs/EXP01/results.md`,
`calibrate_output.log`, `bench_readonly_results.json`, `random_baseline_results.json`.

### G2 — Eval-Harness / Production Rule-Path Consistency Audit (EXP02, strong)
8/8 scenario agreement between `eval.py` and `backend/main.py`'s `apply_rule_override`, confirmed
at module-identity level (both resolve to the exact same in-memory rules module object) — stronger
than the success criterion required. Source: `experiment_runs/EXP02/results.md`,
`exp02_output.json`.

### G3 — Behavioral Measurement of the Dual-Layer Decision System (EXP03, minimum_viable)
**Central complication, reported honestly.** 4-bucket distribution over AQUA-1B's 8 eval.py
scenarios: unparseable-defaulted-to-ok 4/8 (50%), parseable-and-agrees 3/8 (38%),
parseable-and-under-calls-and-escalated 1/8 (12%), parseable-and-over-calls 0/8 (0%). The
dominant observed pathway is **malformed-output fail-safe defaulting**, not sophisticated
severity-correction — matching the pre-registered minimum-viable framing, not the strong framing's
implicit hope of clean correction dominance. `backend/test_decision.py` mechanism-correctness
baseline (18/18 passed) is kept strictly separate from this behavioral finding. Non-enumerable
failure mode for H2: the model **fabricates specific numeric sensor values** in free-text
reasoning in 6/8 scenarios (e.g., states DO=6.0 mg/L when the actual reading was 5.7), invisible to
a status-only comparison and occurring even inside the "agrees" bucket — n=8, qualitative,
single-rater, illustrative only. Adapter provenance is mtime-only (gitignored, no commit history).
Source: `experiment_runs/EXP03/results.md`, `g3_results.json`.

### G4 — Agentic Tool-Routing Negative-Result Robustness (EXP04, strong)
Fresh re-run plus 4 genuinely new scenarios (n=5→n=9) preserves and strengthens the negative
result: AQUA-1B 0%/0% format/selection on both n=5 and n=9; AQUA-7B's constant-answer flag holds
true on both n=5 (5/5 `get_sensor_trend`) and n=9 (9/9). Disclosed discrepancy: published README
table states AQUA-7B format=60%/mean steps=3.6; this run measures 100%/2.0 with no configuration
difference found to explain it (likely `mlx-lm`/model-stack drift) — footnoted, not smoothed over.
Source: `experiment_runs/EXP04/results.md`, `aqua1b_results.jsonl`, `aqua7b_results.jsonl`.

### G5 — Vision Dataset Leakage Audit (EXP05, minimum_viable)
Strong criterion (zero cross-split leakage beyond ogretmen) **not met**: perceptual hashing found
32 near-duplicate train/val pairs at a strict threshold, 14 of them adjacent frame indices of the
same source video — a real, milder, previously undocumented temporal-adjacency leak distinct from
the already-corrected ogretmen case (0 exact frame-index overlaps; ogretmen positive control
independently confirmed the detection method). Minimum-viable met: affected files fully enumerated.
**Live gap:** per EXP11's cross-check, this caveat is not yet reflected anywhere the headline
mAP50/precision/recall numbers are reported in `paper_workspace` — must be closed before freeze.
Source: `experiment_runs/EXP05/results.md`, `exp05_summary.json`.

### G6 — Export-Format Accuracy Loss: Mechanism and Reproducibility (EXP06, strong)
Six-configuration table reproduced (onnx & torchscript both mAP50=0.8291, ΔmAP50=-0.0104).
CoreML/ANE latency measured across 3 independent sessions: p50 9.03ms±0.22, p95 9.60ms±0.18 — a
genuine multi-session variance report. New per-image ONNX-vs-TorchScript diff found near-bit-
identical agreement (0/98 images meaningfully different, median IoU 0.99999976) — stronger direct
evidence than anticipated for a systematic shared accuracy loss, though full NMS-specific isolation
(vs. other export-vs-eager differences) remained out of scope. Source:
`experiment_runs/EXP06/results.md`, `run1_full_table.json`, `g6_onnx_ts_diff_summary.json`.

### G7 — Vision Operating-Point Safety Trace (EXP07, strong)
Fish-per-frame distribution from val labels (min=3, avg=15.56); empirical full-frame-miss rate at
deployed conf=0.20 is 0/98. **Must-disclose finding:** `model.val(conf=X)` does not pin
box.mp/box.mr to X below the internal argmax; reading the fine-grained curve directly reveals the
actual deployed operating point (conf=0.20) runs at **P=0.720/R=0.782**, materially different from
the headline academic-argmax **P=0.858/R=0.719** the rest of the manuscript cites. Both are
correct for different questions — the manuscript currently has no language distinguishing them, and
the H7 safety discussion must cite 0.782, not 0.719. Second finding: the val set has **zero**
frames with k=1 or k=2 fish (min=3) — the sparse-frame regime is entirely untested, not merely
rare; extrapolated miss-risk at k=1 is ~22–28%. Source: `experiment_runs/EXP07/results.md`,
`exp07_conf_curve_summary.json`, `exp07_fish_per_frame_summary.json`.

### G8 — MLOps PSI Drift-Binning Systematic Sweep & Three-Way Gate Exercise (EXP08, minimum_viable)
16-point severity sweep (Δ0.00–0.30) through quantile-edge and equal-width PSI binning. The
**literal H5 direction is inverted at low severities**: quantile is more sensitive than equal-width
at Δ≤0.06 (Δ=0.02: quantile 0.164 "moderate" vs. equal-width 0.078 "none") — equal-width only
overtakes and saturates from Δ≥0.08 onward (Δ=0.30: quantile 4.844 vs. equal-width 7.649),
mechanism confirmed via per-bin decomposition (a large fixed equal-width bin emptying). This is the
pre-registered minimum-viable failure branch — the qualitative shape claim needed correcting, not
confirming. Three-way `retrain.decide()` gate exercised correctly (no-drift→none, PSI=0.0048;
significant-drift→retrain, PSI=0.9070). Self-flagged internal inconsistency: the sweep's own
Δ=0.04 entry (PSI=0.7279) differs from the separately-drawn Δ=0.04 significant-drift window
(PSI=0.9070) due to unseeded per-call resampling — low severity, footnoted. Source:
`experiment_runs/EXP08/results.md`, `exp08_output.json`.

### G9 — twin_bridge Mechanism-Evidence Inventory (EXP09, strong)
Fallback goal; success criteria equal its own minimum-viable ceiling, fully reached. Existing
`test_bridge.py` suite: 18 passed, 1 failed — root-caused (not concealed) to a test-helper
float-truncation bug (`int(8.12*100)` truncates to 811 in IEEE-754), not a production `decode()`
error. 4 new FakeTwin-scripted edge-case tests authored and passed, correctly labeled as
team-authored mechanism evidence, never described as cross-implementation corroboration.
`twin_bridge/` confirmed untracked in git (no commit history); no live Godot/CODESYS session
exists anywhere in the repo — TB-3 correctly excluded, not attempted. Source:
`experiment_runs/EXP09/results.md`, `existing_test_bridge_run.log`, `new_edge_case_test_run.log`.

### G10 — Cross-Layer Thesis Framing & Confirmatory Literature Check (EXP10, strong)
Confirmatory search (CT-4) found no closer prior instance of the cross-layer thesis (C2); C2
remains OPEN, confidence medium — correctly not over-claimed off a negative search. Framing CT-3
(hybrid hook + hedged thesis) selected, refined against the real EXP03/EXP07/EXP08 findings. Of
EXP01–EXP09: 4/9 held cleanly or strengthened, 1/9 central complication (EXP03), 2/9 required
directional/numeric correction (EXP07, EXP08), 1/9 honestly-incomplete as pre-committed (EXP09),
1/9 new orthogonal finding (EXP05). Refined claim: the deterministic layer's most commonly realized
function is unconditional fail-safe defaulting under malformed/uncertain output (50%, EXP03), not
sophisticated correction (only 12%) — narrower and more precise than the original narrative, not a
retreat. Source: `experiment_runs/EXP10/results.md`, `exp10_cross_layer_synthesis.md`,
`exp10_literature_search.md`.

### G11 — Manuscript-Wide Data-Integrity & Provenance Discipline (EXP11, strong)
Grep pass confirms the stale 0.695 recall figure never ships as a live claim: 4 occurrences total,
3 historical/safe, **1 live/unfixed** — `llm-service/knowledge/06-davranis-ve-refah-gostergeleri.md:58`
states recall "~0.695" as a live fact, and this file **is ingested into the production RAG
knowledge base**, making it a retrievable, user-facing stale claim (HIGH-priority disclosure; not
fixed, per the read-only guardrail). 0.719 confirmed correct everywhere else it is cited.
Provenance coverage reaches 10/10 experiments (commit `3c1b9fa` + date per number, up from 3/10).
Finalization surfaced 5 additional undocumented stale/inconsistent figures: the 0.719-vs-0.782
recall ambiguity (G7), a 0.858-vs-0.859 precision rounding inconsistency (propagated to
README.md/README.tr.md), the README agent-benchmark table drift (G4), EXP08's internal PSI
resampling inconsistency, and EXP05's leakage caveat not yet propagated to headline vision numbers.
Recall figure's full provenance chain verified via git log: introduced by `d709836`, corrected by
`9a260af`. Source: `experiment_runs/EXP11/results.md`, `provenance_table.json`, `grep_output.txt`.

## 3. Theory Track — Explicit Non-Gap Statement

Per `track_decomposition.json`, the theory track was never scheduled: `recommended_track` is
`"empirical"` and `theory_questions` is `[]`. This reflects a deliberate decision at the
brainstorm/formalize_goals phase (`brainstorm.json`'s `track_recommendation.needs_theory_track =
false`), not an incomplete or abandoned track. The absence of `math_workspace/` is the expected,
correct state of this project and requires no remediation. See
`paper_workspace/theory_track_summary.json` for the structured record.

## 4. Cross-Cutting "Honest Complications" Carried Forward (must not be smoothed over downstream)

1. **EXP03 (G3):** malformed-defaults-safe (50%) is the dominant observed pathway, not
   severity-correction (only 12%); plus the fabricated-sensor-values non-enumerable failure mode
   (6/8 scenarios, including inside the "agrees" bucket).
2. **EXP05 (G5):** near-duplicate train/val leakage (32 pairs, 14 adjacent-frame) beyond ogretmen,
   not yet propagated as a caveat next to the headline vision numbers.
3. **EXP07/EXP11 (G7):** the 0.719 (F1-argmax) vs. 0.782 (actual deployed conf=0.20 recall)
   distinction — both correct, currently undisclosed which governs the H7 safety discussion (should
   be 0.782), plus the zero-sparse-frame dataset-coverage gap.
4. **EXP08 (G8):** the PSI literal-direction inversion at low severity, and the internal
   Δ=0.04 sweep-vs-gate-exercise discrepancy from unseeded resampling.
5. **EXP11:** the live, unfixed 0.695 recall figure inside the production RAG knowledge base
   (`llm-service/knowledge/06-davranis-ve-refah-gostergeleri.md:58`).

These are pre-registered, honestly-reported outcomes per `verification_handoff.md` — not defects
in the experiment track — and are organized here faithfully for the next pipeline stage
(formalize_results / duality_check) to evaluate and resolve in the manuscript.

## 5. Recommended Presentation Order (carried from verification_handoff.md, for downstream reference)

EXP02 → EXP03 → EXP04 → EXP01 → EXP06 → EXP07 → EXP05 → EXP08 → EXP09 → EXP10 → EXP11.
